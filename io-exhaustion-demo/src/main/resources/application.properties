spring.application.name=io-exhaustion-demo

management.endpoints.web.exposure.include=health,prometheus,metrics

# Small pool + backlog so the thread-starvation bug is reproducible with
# modest curl concurrency instead of needing hundreds of real clients.
server.tomcat.threads.max=10
server.tomcat.accept-count=10
